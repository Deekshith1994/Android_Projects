<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kdeek.inclass11">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE">
        0
    </uses-permission>
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EmailPasswordActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUp" />
        <activity android:name=".Chat" />
        <activity android:name=".VisitPlaces" />
        <activity android:name=".Venues" />
        <activity android:name=".Details" />
        <activity android:name=".FavCities" />
        <activity android:name=".Visited"></activity>
    </application>

</manifest>